# Environment variables – copy to .env and fill in for local development.
# Never commit a populated .env file to source control.

# Lambda log verbosity: DEBUG | INFO | WARNING | ERROR
LOG_LEVEL=INFO

# Outbound webhook for the daily quote (Slack or Discord incoming webhook URL).
# Leave blank to disable delivery – the quote is still logged to CloudWatch.
NOTIFY_WEBHOOK_URL=
